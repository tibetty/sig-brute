/**
 * Shared ABI layout primitives (offset tables, head dynamic slots) used by skeleton and body
 * decoders. Strategy-specific choice policy stays in {@code decode.strategy.body}.
 */
package me.tibetty.sigbrute.decode.layout;
